name: Update IPTV playlist
on:
  schedule:
    - cron: '23 4 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'iptv_filter.py'
      - 'tests/**'
      - 'extra_channels.json'
      - 'dyvy_channels.json'
      - 'content_origin_policy.json'
      - '.github/workflows/update.yml'
permissions:
  contents: write
concurrency:
  group: playlist-update
  cancel-in-progress: false
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Generate and validate playlist
        run: |
          python -m unittest discover -s tests
          python iptv_filter.py
          python checks/policy_regression.py
      - name: Publish playlist and update timestamp
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add my-iptv.m3u status.json channel-audit.json origin-filter-report.json
          if ! git diff --cached --quiet; then
            git commit -m "Refresh IPTV playlist"
            git push
          fi
